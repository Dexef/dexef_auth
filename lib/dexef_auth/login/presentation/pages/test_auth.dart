import 'package:flutter/material.dart';

class TestAuth extends StatelessWidget {
  const TestAuth({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text("assssssssssssssssssssss"),
    );
  }
}
